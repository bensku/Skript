#
# Allows players with the 'skript.teleport' permission to be able to teleport
# with a compass like in WorldEdit.
#

on right click with compass:
	player has permission "skript.teleport"
	loop blocks above targeted block where [input isn't solid]:
		teleport player to block above loop-block
		stop trigger
